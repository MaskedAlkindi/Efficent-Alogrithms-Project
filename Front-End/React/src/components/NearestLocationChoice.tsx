import { FunctionComponent } from "react";
import styles from "./NearestLocationChoice.module.css";
const NearestLocationChoice: FunctionComponent = () => {
  return <div className={styles.nearestChoice}>Nearest [Choice]</div>;
};

export default NearestLocationChoice;
